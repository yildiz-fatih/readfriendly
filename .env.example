# SERVER
PORT=8080
# GOTENBERG
GOTENBERG_URL=http://localhost:3000
# PANDOC
PANDOC_URL=http://localhost:4000
# POSTGRES
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=readfriendly
DATABASE_URL=postgres://postgres:youshallnotpass@localhost:5432/readfriendly?sslmode=disable # only needed for local development
# AWS
AWS_REGION=changeme
AWS_ACCESS_KEY_ID=changeme
AWS_SECRET_ACCESS_KEY=changeme
S3_BUCKET_NAME=changeme
# RABBITMQ
RABBITMQ_USER=guest
RABBITMQ_PASS=guest
RABBITMQ_URL=amqp://guest:guest@localhost:5672 # only needed for local development
# SMTP
SMTP_HOST=changeme
SMTP_PORT=changeme
SMTP_USER=changeme
SMTP_PASS=changeme
SMTP_FROM=changeme
